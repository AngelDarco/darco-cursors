import { CustomCursorsProps } from "../types";
import styles from "../components/cursor-styles.module.css";

const CursorsStyles: CustomCursorsProps = {
  general: styles.general,
  pointer: styles.pointer,
  text: styles.text,
};

export default CursorsStyles;
